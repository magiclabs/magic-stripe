import { EffectCallback } from 'react';
/**
 * Executes the given effect on an interval.
 */
export declare function useInterval(effect: EffectCallback, deps: readonly any[], interval?: number): () => void;
