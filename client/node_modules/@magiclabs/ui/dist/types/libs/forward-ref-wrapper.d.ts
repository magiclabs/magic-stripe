import { ForwardRefExoticComponent, ForwardRefRenderFunction, PropsWithoutRef, RefAttributes } from 'react';
/**
 * Wraps `React.forwardRef` and applies a semantic `displayName` and
 * `defaultProps` without side effects.
 *
 * This wrapper is required to properly tree-shake `React.forwardRef`
 * components, otherwise the minifier can't understand that `defaultProps` and
 * `displayName` are pure.
 */
export declare function forwardRefWrapper<T, P = {}>(name: string, render: ForwardRefRenderFunction<T, P>): ForwardRefExoticComponent<PropsWithoutRef<P> & RefAttributes<T>>;
export declare function forwardRefWrapper<T, P = {}>(name: string, defaultProps: Partial<PropsWithoutRef<P> & RefAttributes<T>> | undefined, render: ForwardRefRenderFunction<T, P>): ForwardRefExoticComponent<PropsWithoutRef<P> & RefAttributes<T>>;
