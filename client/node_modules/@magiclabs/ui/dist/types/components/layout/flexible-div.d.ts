import React from 'react';
import { ResizePayload } from 'watch-resize';
interface TransitionStyles {
    transition: string;
    height: string;
    width: string;
    willChange: string;
}
interface TransitionConfig {
    duration?: string;
    timingFunction?: string;
    delay?: string;
}
interface FlexibleDivProps extends React.HTMLAttributes<HTMLDivElement> {
    initialDelay?: number;
    transition?: boolean | TransitionConfig;
    applyCSSVariables?: {
        element?: HTMLElement;
        transitionVar?: string;
        heightVar?: string;
        widthVar?: string;
    };
    onResize?: (payload: Omit<ResizePayload<HTMLElement>, 'destroy'> & {
        transitionStyles: TransitionStyles;
    }) => void;
    onAnimationFrame?: FrameRequestCallback;
    observerOnly?: boolean;
    autoWidth?: boolean;
    autoHeight?: boolean;
}
/**
 * This component is a container which automatically updates its `height` style
 * property based on the total size of its child nodes. This enables dynamic
 * transitional height behavior.
 */
export declare const FlexibleDiv: React.ForwardRefExoticComponent<FlexibleDivProps & React.RefAttributes<HTMLDivElement>>;
export {};
