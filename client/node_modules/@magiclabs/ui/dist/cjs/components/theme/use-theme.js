'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var createTheme = require('../../libs/theme/create-theme.js');
var defaultTheme = require('../../libs/theme/default-theme.js');
require('@artsy/fresnel');

const ThemeContext = /* @__PURE__ */ React.createContext({
    theme: null,
});
function useTheme(options) {
    var _a;
    const theme = (_a = React.useContext(ThemeContext).theme) !== null && _a !== void 0 ? _a : defaultTheme.DEFAULT_THEME;
    if (options === null || options === void 0 ? void 0 : options.inverseSemantics) {
        const themeWithInverseSemantics = createTheme.createTheme(Object.assign(Object.assign({}, theme.config), { inverseSemantics: true }));
        return themeWithInverseSemantics;
    }
    return theme;
}

exports.ThemeContext = ThemeContext;
exports.useTheme = useTheme;
//# sourceMappingURL=use-theme.js.map
