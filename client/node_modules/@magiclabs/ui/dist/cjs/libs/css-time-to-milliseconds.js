'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function cssTimeToMilliseconds(timeString) {
    var _a;
    if (!timeString)
        return 0;
    const num = parseFloat(timeString);
    /* eslint-disable-next-line @typescript-eslint/prefer-regexp-exec */
    const unit = (_a = timeString.match(/m?s/)) === null || _a === void 0 ? void 0 : _a[0];
    switch (unit) {
        case 's':
            return num * 1000;
        case 'ms':
            return num;
        default:
            return 0;
    }
}

exports.cssTimeToMilliseconds = cssTimeToMilliseconds;
//# sourceMappingURL=css-time-to-milliseconds.js.map
