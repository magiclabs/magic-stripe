import React from 'react';

const TabPanel = (props) => {
    const { id, tabID, selected, children } = props;
    return (React.createElement("div", { id: id, role: "tabpanel", "aria-labelledby": tabID, tabIndex: 0, style: { display: selected ? 'block' : 'none' } }, selected ? children : null));
};

export { TabPanel };
//# sourceMappingURL=tab-panel.js.map
