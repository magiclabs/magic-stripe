import React from 'react';
import { withDesign } from 'storybook-addon-designs';
import { StoryMetadata, Story } from '../../types/storybook-metadata';
import { Alert } from '../../components/feedback';

export default {
  title: 'Feedback/Alert',
  component: Alert,
  decorators: [withDesign],
} as StoryMetadata;

export const AlertStory: Story = (args) => {
  return <Alert {...args}>This is an alert!</Alert>;
};

AlertStory.storyName = 'Alert';

AlertStory.parameters = {
  design: {
    type: 'figma',
    url: 'https://www.figma.com/file/vn0cdq2HFXKIEMyVHQNFpr/%E2%9D%96-UI-Components-v2?node-id=592%3A228',
  },
};
