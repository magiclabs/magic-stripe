import React from 'react';
import { StoryMetadata, Story } from '../../types/storybook-metadata';
import { DuotoneIcon, DuotoneIcons } from '../../components/icons';
import { Outset, FlexRow } from '../../components/layout';

export default {
  title: 'Icons/Duotone Icons',
  component: DuotoneIcon,
} as StoryMetadata;

export const DuotoneIconStory: Story = () => {
  return (
    <FlexRow wrap>
      <Outset all={20}>
        {Object.entries(DuotoneIcons).map(([key, iconDef]) => {
          return (
            <div>
              <DuotoneIcon size={50} type={iconDef} key={key} />
            </div>
          );
        })}
      </Outset>
    </FlexRow>
  );
};

DuotoneIconStory.argTypes = {};

DuotoneIconStory.storyName = 'Duotone Icons';
