/* eslint-disable react/button-has-type, jsx-a11y/anchor-is-valid, jsx-a11y/no-noninteractive-tabindex */
import React from 'react';
import { StoryMetadata, Story } from '../../types/storybook-metadata';
import { Linkable } from '../../components/primitives';

export default {
  title: 'Primitives/Links',
  component: Linkable,
} as StoryMetadata;

export const AnchorLinkStory: Story = (args) => {
  return (
    <Linkable {...args}>
      <a tabIndex={0}>I'm an anchor link!</a>
    </Linkable>
  );
};

AnchorLinkStory.storyName = 'Link w/Anchor element';

export const ButtonLinkStory: Story = (args) => {
  return (
    <Linkable {...args}>
      <button>I'm a button link!</button>
    </Linkable>
  );
};

ButtonLinkStory.storyName = 'Link w/Button element';
