import React from 'react';
import { action } from '@storybook/addon-actions';
import { StoryMetadata, Story } from '../../types/storybook-metadata';
import { TextButton } from '../../components/primitives';
import { MonochromeIcons } from '../../components/icons';

export default {
  title: 'Primitives/Text Button',
  component: TextButton,
} as StoryMetadata;

export const TextButtonStory: Story = (args) => {
  return (
    <TextButton leadingIcon={MonochromeIcons.Document} {...args} onPress={action('Clicked')}>
      hello world
    </TextButton>
  );
};

TextButtonStory.argTypes = {
  leadingIcon: { control: { type: null } },
  trailingIcon: { control: { type: null } },
};

TextButtonStory.storyName = 'Text Button';
